name: architecture
description: Base architecture components for the app.
version: 1.0.0
resolution: workspace
publish_to: 'none'

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
